http:
  routers:
    s3-minio:
      entryPoints:
      - https
      service: s3-service
      rule: Host(`s3.domain.ru`)
      tls:
        certResolver: letsEncrypt
  services:
    s3-service:
      loadBalancer:
        servers:
        - url: http://192.168.1.3:9000
